export const initSearch=()=>{const makeSelect=document.getElementById('make');const modelSelect=document.getElementById('model');const engineSelect=document.getElementById('engine');fetch('/data/car-makes.json').then(response=>response.json()).then(data=>{populateMakes(data.makes)});populateYears();populateEngineTypes();populateCategories();makeSelect.addEventListener('change',handleMakeChange);modelSelect.addEventListener('change',handleModelChange)};const populateMakes=(makes)=>{const makeSelect=document.getElementById('make');makes.forEach(makeObj=>{const option=document.createElement('option');option.value=makeObj.name;option.textContent=makeObj.name;makeSelect.appendChild(option)})};const populateModels=(models)=>{const modelSelect=document.getElementById('model');modelSelect.innerHTML='<option value="">Select Model</option>';models.forEach(model=>{const option=document.createElement('option');option.value=model;option.textContent=model;modelSelect.appendChild(option)});modelSelect.disabled=!1};const handleMakeChange=(event)=>{const make=event.target.value;const modelSelect=document.getElementById('model');if(make){fetch(`/data/models/${make.toLowerCase()}.json`).then(response=>response.json()).then(data=>{populateModels(data.models)})}else{modelSelect.innerHTML='<option value="">Select Model</option>';modelSelect.disabled=!0}};const handleModelChange=(event)=>{const engineSelect=document.getElementById('engine');if(event.target.value){engineSelect.disabled=!1}else{engineSelect.disabled=!0}};const populateYears=()=>{const yearSelect=document.getElementById('year');yearSelect.innerHTML='<option value="">Select Year</option>';const years=[2020,2021,2022,2023];years.forEach(year=>{const option=document.createElement('option');option.value=year;option.textContent=year;yearSelect.appendChild(option)})};const populateEngineTypes=()=>{const engineSelect=document.getElementById('engine');engineSelect.innerHTML='<option value="">Engine Type</option>';const engines=['Petrol','Diesel','Hybrid','Electric'];engines.forEach(engine=>{const option=document.createElement('option');option.value=engine;option.textContent=engine;engineSelect.appendChild(option)});engineSelect.disabled=!1};const populateCategories=()=>{const categorySelect=document.getElementById('category');categorySelect.innerHTML='<option value="">Part Category</option>';const categories=['Brakes','Suspension','Engine','Transmission','Exhaust'];categories.forEach(category=>{const option=document.createElement('option');option.value=category.toLowerCase();option.textContent=category;categorySelect.appendChild(option)})};export const searchParts=async(criteria)=>{}